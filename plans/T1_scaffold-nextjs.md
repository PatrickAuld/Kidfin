# T1 — Scaffold Next.js + Tailwind (Plan)

Goal: Create a clean Next.js (App Router) + Tailwind project skeleton for Kidfin (in ) using the **latest stable** dependencies.

## Commands / Steps

1) Scaffold app (latest create-next-app)

- Use TypeScript
- Use App Router
- Enable Tailwind
- Include ESLint

2) Verify local dev works

- `npm run dev`
- `npm run build`

3) Establish minimal repo conventions

- Ensure `.gitignore` includes `.next`, `node_modules`, `.env*`
- Ensure `README.md` points to `plans/PROJECT_PLAN.md`

## Outputs

- Next.js project files (app router)
- Tailwind configured
- TypeScript configured
- ESLint configured
- Build passes
